version: "3.8"

services:
  image-uploader:
    build: .
    ports:
      - "8080:8080"
    environment:
      AWS__AccessKey: ${AWS_ACCESS_KEY}
      AWS__SecretKey: ${AWS_SECRET_KEY}
      AWS__Region: eu-central-1
      AWS__BucketName: tasos-image-uploader-dev